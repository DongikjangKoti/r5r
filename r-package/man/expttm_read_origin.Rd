% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ttm_db.R
\name{expttm_read_origin}
\alias{expttm_read_origin}
\title{Decode one origin's expTTM payload into a data.frame in LEGACY column order.
Times come back as tenths/10, which is bit-identical to the legacy doubles
because PathBreakdown getters already round to one decimal place.}
\usage{
expttm_read_origin(
  con,
  scenario_id,
  origin_id,
  from_id = origin_id,
  apply_cutoff_na = TRUE
)
}
\description{
Decode one origin's expTTM payload into a data.frame in LEGACY column order.
Times come back as tenths/10, which is bit-identical to the legacy doubles
because PathBreakdown getters already round to one decimal place.
}
